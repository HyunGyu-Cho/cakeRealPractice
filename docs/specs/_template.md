# <도메인명> 스펙

> 이 파일을 `docs/specs/<도메인>.md`로 복사해 작성한다. 구현 착수 전에 내용을 확정해야 하며,
> 스펙 없는 도메인의 새 클래스 생성은 훅이 차단한다. 확정 과정에서 team-plan.md 8장(핵심 비즈니스 규칙)의
> 관련 미결 항목이 있으면 함께 채우고, 상태값을 새로 정하면 conventions.md 인벤토리도 갱신한다.

## 1. 개요와 유스케이스

- 이 도메인이 해결하는 문제 한 줄:
- 주요 유스케이스 (액터 → 행동 → 결과):
  1.
  2.

## 2. 상태값 (conventions.md 상태값 공통 규칙 준수)

| 컬럼 | 값(영문 enum 이름) | 시작 상태 | 최종 상태 | 전이 규칙 요약 |
|---|---|---|---|---|
| | | | | |

- status가 아닌 것 점검: 불리언인가? 파생값인가? type/category인가? 다른 도메인의 status인가?

## 3. DB

- 사용할 테이블 (V0 기준):
- 스키마 변경 필요 여부: 필요 시 "증분 V파일 + V0/V1 소급 반영" 규칙 준수

## 4. 도메인 간 인터페이스

- 내가 제공할 공개 Service 메서드:
- 내가 사용할 다른 도메인의 공개 Service 메서드 (Mapper 직접 호출 금지):

## 5. 화면

- 대상 화면과 URL (목업 → 실구현 전환 시 URL·템플릿 유지):
- 목업 JS가 시연하는 임시 동작 중 규칙으로 확정할 것:

## 6. 비즈니스 규칙 확정

- team-plan.md 8장에서 이 도메인과 관련된 항목:
- 확정한 규칙 (확정 후 team-plan 8장 표 갱신):

## 7. 완료 기준

- [ ] 정상 흐름·주요 실패 흐름 동작
- [ ] 입력 검증(form DTO)과 접근 권한 적용
- [ ] 상태 전이·트랜잭션 규칙 준수
- [ ] 전용 테스트 통과 (Service + Controller, store 패턴)
- [ ] 관련 SQL·문서 함께 수정
