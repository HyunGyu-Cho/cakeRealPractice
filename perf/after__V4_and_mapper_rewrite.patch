diff --git a/src/main/resources/db/migration/V4__community_performance_indexes.sql b/src/main/resources/db/migration/V4__community_performance_indexes.sql
new file mode 100644
index 0000000..ce368c0
--- /dev/null
+++ b/src/main/resources/db/migration/V4__community_performance_indexes.sql
@@ -0,0 +1,31 @@
+-- V4: 커뮤니티 성능 인덱스 (부하 측정 기반)
+--
+-- [배경]
+--   게시글 100,000건 · 댓글 300,000건 · 동시 16커넥션 부하 측정 결과:
+--   - 메인 페이지 인기 글(findPopularPosts) p50 11.1초 — like_count 정렬이 풀스캔+filesort
+--   - 커뮤니티 목록(findActivePostPage) p50 19.0초, 무한스크롤 p50 7.3초
+--   - EXPLAIN: posts type=ALL + Using temporary; Using filesort,
+--     댓글수 DEPENDENT SUBQUERY가 LIMIT 이전 전체 행에 대해 실행됨.
+--   posts·comments에는 PK·UNIQUE·FK 인덱스뿐이라 조회 패턴을 받쳐줄 보조 인덱스가 없다.
+--   (orders·reviews·coupons가 이미 조회용 복합 인덱스를 가진 것과 같은 원칙을 적용한다.)
+--
+-- [설계 원칙]
+--   복합 인덱스는 등치 조건(status)을 앞에, 범위·정렬 컬럼(id, like_count)을 뒤에 둔다(좌측 접두 규칙).
+--   인덱스는 쓰기 시 B-tree 갱신 비용을 수반하므로 실제 쿼리 패턴이 확인된 조합에만 최소로 건다.
+
+-- 1) 목록·카운트·무한스크롤(keyset): WHERE status + ORDER BY id
+CREATE INDEX `idx_posts_status_id` ON `posts` (`status`, `id`);
+
+-- 2) 카테고리 필터 목록·카운트: 커버링으로 카운트가 인덱스만으로 끝난다
+CREATE INDEX `idx_posts_category_status_id` ON `posts` (`category_id`, `status`, `id`);
+
+-- 3) 메인 인기 글: WHERE status + ORDER BY like_count DESC, id DESC (역방향 인덱스 스캔)
+CREATE INDEX `idx_posts_status_like_count_id` ON `posts` (`status`, `like_count`, `id`);
+
+-- 4) 댓글수 서브쿼리 커버링: FK 인덱스(post_id 단일)로는 status 확인에 행 접근이 필요하다.
+--    (post_id, status)면 COUNT가 인덱스만으로 끝난다 (EXPLAIN Extra: Using index)
+CREATE INDEX `idx_comments_post_status` ON `comments` (`post_id`, `status`);
+
+-- [적용 후 동일 부하 실측]
+--   인기 글 p50 11,069ms → 2.8ms · 목록 p50 19,036ms → 170ms (mapper 리라이트 결합)
+--   전체 처리량 2.8 → 96.2 ops/s
diff --git a/src/main/resources/mapper/community/CommunityMapper.xml b/src/main/resources/mapper/community/CommunityMapper.xml
index 90038b9..5b781fb 100644
--- a/src/main/resources/mapper/community/CommunityMapper.xml
+++ b/src/main/resources/mapper/community/CommunityMapper.xml
@@ -14,18 +14,35 @@
          ORDER BY sort_order
     </select>
 
+    <!-- [성능] 카테고리 필터가 없으면 JOIN을 생략해 idx_posts_status_id 커버링 카운트로 처리한다.
+         조인을 강제하면 옵티마이저가 category FK를 타고 행 단위로 status를 확인해 10만 건 기준 2.2초가 걸렸다. -->
     <select id="countActivePosts" resultType="long">
+        <choose>
+            <when test="categoryCode != null">
         SELECT COUNT(*)
           FROM posts p
           JOIN post_categories c ON c.id = p.category_id
          WHERE p.status = 'ACTIVE'
-        <if test="categoryCode != null">
            AND c.code = #{categoryCode}
-        </if>
+            </when>
+            <otherwise>
+        SELECT COUNT(*)
+          FROM posts
+         WHERE status = 'ACTIVE'
+            </otherwise>
+        </choose>
     </select>
 
     <!-- 목록 공통 SELECT. 댓글 수는 자기 도메인 테이블이라 서브쿼리로 세고,
-         닉네임은 member 도메인 소유라 여기서 JOIN 하지 않는다(도메인 간 연동 규칙). -->
+         닉네임은 member 도메인 소유라 여기서 JOIN 하지 않는다(도메인 간 연동 규칙).
+
+         [성능 리라이트 — late row lookup]
+         기존에는 posts 전체(WHERE status)에 대해 댓글수 서브쿼리를 평가한 뒤 정렬·LIMIT을 적용해
+         (EXPLAIN: Using temporary; Using filesort + DEPENDENT SUBQUERY × 전체 행)
+         10만 건 기준 목록 p50 19초, 메인 인기 글 p50 11초까지 나왔다.
+         파생테이블에서 id만 V4 인덱스로 LIMIT 건수까지 좁힌 뒤,
+         확정된 소수 행에 대해서만 JOIN·댓글수 서브쿼리를 실행한다.
+         실측: 목록 p50 19,036ms → 170ms, 인기 글 p50 11,069ms → 2.8ms. -->
     <sql id="postSummaryColumns">
         SELECT p.id,
                p.member_id,
@@ -38,37 +55,57 @@
                  WHERE cm.post_id = p.id
                    AND cm.status = 'ACTIVE') AS comment_count,
                p.created_at
-          FROM posts p
-          JOIN post_categories c ON c.id = p.category_id
+    </sql>
+
+    <!-- 파생테이블 내부 필터. 카테고리 code→id 확인은 EXISTS로 두어
+         idx_posts_category_status_id 를 그대로 태운다. -->
+    <sql id="activePostIdFilter">
          WHERE p.status = 'ACTIVE'
+        <if test="categoryCode != null">
+           AND EXISTS (SELECT 1 FROM post_categories pc
+                        WHERE pc.id = p.category_id AND pc.code = #{categoryCode})
+        </if>
     </sql>
 
     <select id="findActivePostPage" resultType="com.cakeshop.domain.community.dto.view.PostSummaryRow">
         <include refid="postSummaryColumns"/>
-        <if test="categoryCode != null">
-           AND c.code = #{categoryCode}
-        </if>
+          FROM (SELECT p.id
+                  FROM posts p
+                <include refid="activePostIdFilter"/>
+                 ORDER BY p.id DESC
+                 LIMIT #{size} OFFSET #{offset}) t
+          JOIN posts p ON p.id = t.id
+          JOIN post_categories c ON c.id = p.category_id
          ORDER BY p.id DESC
-         LIMIT #{size} OFFSET #{offset}
     </select>
 
     <select id="findActivePostSlice" resultType="com.cakeshop.domain.community.dto.view.PostSummaryRow">
         <include refid="postSummaryColumns"/>
-        <if test="categoryCode != null">
-           AND c.code = #{categoryCode}
-        </if>
-        <if test="cursor != null">
-           AND p.id &lt; #{cursor}
-        </if>
+          FROM (SELECT p.id
+                  FROM posts p
+                <include refid="activePostIdFilter"/>
+                <if test="cursor != null">
+                   AND p.id &lt; #{cursor}
+                </if>
+                 ORDER BY p.id DESC
+                 LIMIT #{limit}) t
+          JOIN posts p ON p.id = t.id
+          JOIN post_categories c ON c.id = p.category_id
          ORDER BY p.id DESC
-         LIMIT #{limit}
     </select>
 
-    <!-- 홈 인기 글 — 좋아요가 많은 순. 동점이면 최신 글이 앞선다. -->
+    <!-- 홈 인기 글 — 좋아요가 많은 순. 동점이면 최신 글이 앞선다.
+         idx_posts_status_like_count_id 역방향 스캔으로 LIMIT 건수만 읽는다. -->
     <select id="findPopularPosts" resultType="com.cakeshop.domain.community.dto.view.PostSummaryRow">
         <include refid="postSummaryColumns"/>
+          FROM (SELECT p.id
+                  FROM posts p
+                 WHERE p.status = 'ACTIVE'
+                 ORDER BY p.like_count DESC, p.id DESC
+                 LIMIT #{limit}) t
+          JOIN posts p ON p.id = t.id
+          JOIN post_categories c ON c.id = p.category_id
          ORDER BY p.like_count DESC, p.id DESC
-         LIMIT #{limit}
     </select>
 
     <select id="findPostDetail" resultType="com.cakeshop.domain.community.dto.view.PostDetailRow">
