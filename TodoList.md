# 전체 개발 TODO

> 기능을 하나씩 완성하는 순서 정본. 순서는 의존 관계 기준(앞이 없으면 뒤가 안 됨)이며,
> 각 항목은 `/new-domain` 절차(스펙 확정 → DB → 구현 → 테스트) 한 사이클로 진행한다.
> 항목을 시작할 때 `[ ]` → `[~]`(진행 중), 완료 시 `[x]`로 갱신하고 README 현황표도 함께 갱신한다.

## 완료

- [x] **store** — 매장 관리 (조회·수정·휴무일)
- [x] **community** — 고객 목록·상세·글쓰기(페이징 2방식) + 관리자 제재
- [x] **member** — 회원가입·마이페이지·프로필/비밀번호 수정·탈퇴(soft delete)·상태별 로그인 차단 (스펙 docs/specs/member.md, V5)
- [x] **product** — 고객 목록(필터·정렬·검색·페이징)·상세 + 관리자 CRUD·대표 이미지 + 공개 계약 getSalesInfo + 홈 연결 (스펙 docs/specs/product.md, V6)

## 1단계 — 기반 (모든 도메인의 전제)

(완료 — 2단계로 진행)

## 2단계 — 구매 흐름 (핵심 가치, 서로 강결합 — 여기까지가 "필수")

- [ ] **cart** — localStorage 목업 → DB(`carts`/`cart_items`) 전환
  - 스펙 단계에서 team-plan 8장 🟡 임시 답 확정: 같은 상품 수량 합산, 가격 재검증 방식.
- [ ] **order(일반) + payment** — 픽업 설정 → 주문서 → 결제(모의 또는 토스) → `PAID` 생성 → 관리자 픽업 처리(`READY_FOR_PICKUP → PICKED_UP`)
  - 일반 주문 시작 상태가 PAID라 결제와 주문 생성은 한 몸. 덩어리가 크면 "모의 결제로 주문 생성" / "실 결제 연동"으로 분할.
  - 스펙 단계에서 확정: 재고 차감·복구 시점, 결제 성공↔주문 상태 변경 순서, 취소 주체·시한.
- [ ] **order(수제)** — 옵션 선택 → 요청 → `UNDER_REVIEW` → 관리자 승인/반려(`IN_PRODUCTION`/`REJECTED`)
  - 스펙 단계에서 확정: 반려 사유·재요청 규칙.
- [ ] **coupon** — 관리자 발급 + 회원 쿠폰함 + checkout 적용·복구
  - 스펙 단계에서 확정: `coupons.status` 값(☐), 쿠폰 사용·복구 시점.

## 3단계 — 구매 후 경험 (order 의존)

- [ ] **review** — 픽업 완료 주문 기준 작성 조건·횟수 확정, 작성·숨김 관리
- [ ] **notification** — `NotificationType` enum 확정(현재 TODO), 주문·결제 이벤트별 생성 + 읽음 처리(`is_read BOOLEAN`)

## 4단계 — 부가 기능

- [ ] **chat** — `chat_rooms.status` 정의(☐), 상담 화면. 주문 배지는 orders.status를 빌려 표시(저장 금지)
- [ ] **home 연결** — 각 도메인 완성 시 공개 View를 메인에 연결 (별도 단계가 아니라 각 항목의 마지막 체크)

## 5단계 — 마감

- [ ] **statistics** — 전 도메인 데이터 의존이므로 반드시 마지막
- [ ] **전체 통합 점검** — 시드 정리(로컬 vs RDS), `customer-mockup.js` 의존 0 확인, 전체 테스트, README 현황표 갱신

---

### 원칙

- **필수 = 1·2단계.** coupon까지 끝나면 "주문하고 픽업하는" 서비스가 성립한다. 일정이 밀리면 3단계 이후를 자른다.
- **미결 규칙은 해당 도메인 차례에 청산한다.** team-plan 8장 빈 칸과 conventions 인벤토리 ☐를 각 항목의 스펙 단계에서 확정한다.
